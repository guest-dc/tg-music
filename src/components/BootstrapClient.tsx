"use client"

import { useEffect } from 'react';
import "bootstrap/dist/js/bootstrap.bundle.min.js";

export default function BootstrapClient() {
   useEffect(() => {
      // Bootstrap initializes itself when imported
   }, []);

   return null;
}